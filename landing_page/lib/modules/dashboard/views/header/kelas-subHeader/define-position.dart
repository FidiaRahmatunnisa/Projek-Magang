import 'package:flutter/material.dart';

class DefinePosition extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Definition Position')),
      body: Center(child: Text('This is the Definition Position page')),
    );
  }
}