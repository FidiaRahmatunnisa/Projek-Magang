import 'package:flutter/material.dart';

class Anouncements extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Anouncements')),
      body: Center(child: Text('This is the Anouncements page')),
    );
  }
}
