import 'package:flutter/material.dart';

class OrganizationCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Organization Card')),
      body: Center(child: Text('This is the Organization Card page')),
    );
  }
}